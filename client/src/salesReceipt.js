export default function salesReceipt(props) {
    return (
        <>
            <div className="columns">
                <div className="column">{props.name}</div>
                <div className="column">{props.name}</div>
                <div className="column">{props.name}</div>
            </div>
        </>
    );
}
